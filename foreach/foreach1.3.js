const fruits = ["mango", "papaya", "pineapple", "apple"];

fruits.forEach((element) => {
    if (element === "apple") {
        console.log(`iam eating an ${element}`);
    } else {
        console.log(`iam eating ${element}`);
    }
});